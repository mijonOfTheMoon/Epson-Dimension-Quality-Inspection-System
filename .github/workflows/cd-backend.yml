# name: Build and Deploy Backend to Cloud Run

# on:
#   push:
#     branches: [ "main" ]
#     paths:
#       - backend/**

# jobs:
#   ci:
#     name: CI
#     uses: ./.github/workflows/ci-backend.yml

#   build-and-run:
#     runs-on: ubuntu-latest
#     needs: ci

#     permissions:
#       contents: read
#       id-token: write

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v5.0.0

#       - name: Login to Container Registry
#         uses: docker/login-action@v3.5.0
#         with:
#           username: ${{ vars.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_PAT }}

#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v3.6.0

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3.11.1

#       - name: Extract metadata (tags, labels) for Docker
#         id: meta
#         uses: docker/metadata-action@v5.8.0
#         with:
#           images: ${{ vars.DOCKERHUB_USERNAME }}/${{ vars.BACKEND_SERVICE }}

#       - name: Build and push
#         id: build-and-push
#         uses: docker/build-push-action@v6.18.0
#         with:
#           context: ./backend
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}

#       - id: auth
#         name: Authenticate to Google Cloud
#         uses: google-github-actions/auth@v2.1.10
#         with:
#           project_id: ${{ vars.PROJECT_ID }}
#           workload_identity_provider: ${{ secrets.IDENTITY_PROVIDER }}

#       - name: 'Deploy Backend to Cloud Run'
#         uses: 'google-github-actions/deploy-cloudrun@v2.7.3'
#         with:
#           service: ${{ vars.BACKEND_SERVICE }}
#           region: ${{ vars.REGION }}
#           image: ${{ vars.DOCKERHUB_USERNAME }}/${{ vars.BACKEND_SERVICE }}@${{ steps.build-and-push.outputs.digest }}
